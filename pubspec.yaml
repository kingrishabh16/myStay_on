name: my_project_booking
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  firebase_core: ^2.25.5
  cloud_firestore: ^4.15.6
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  firebase_auth: ^4.19.2
  random_string: ^2.3.1
  protobuf: ^3.1.0
  crypto: ^3.0.3
  fluttertoast: ^8.2.5
  flutter_spinkit: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
